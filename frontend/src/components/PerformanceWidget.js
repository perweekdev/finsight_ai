// TODO: Day 3에 구현 — RAG vs 비RAG 응답시간 비교
export function createPerformanceWidget() {
  const widget = document.createElement('div')
  widget.textContent = '[PerformanceWidget - 구현 예정]'
  return widget
}
